import React from 'react';
import { ThemedView } from '@/components/ThemedView';
import { ThemedText } from '@/components/ThemedText';

export default function About() {
  return (
    <ThemedView>
      <ThemedText type='subtitle'>Sobre nosotros</ThemedText>
    </ThemedView>
  );
}
